<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shayne.even.prisonerssandpit">

    <application
        android:name=".PrisonersSandpitApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.AddPrisonerActivity" />
        <activity
            android:name=".ui.activities.PrisonerHomeActivity"
            android:label="@string/title_activity_prisoner_home" />
        <activity android:name=".ui.activities.TrainerSettingsActivity" />

        <service
            android:name=".service.AgentTrainerService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".ui.activities.TestingActivity"
            android:label="@string/title_activity_testing"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--<activity-->
            <!--android:name=".ui.activitPrisonerSelectDialogvity"-->
            <!--android:theme="@android:style/Theme.Holo.Light.Dialog" />-->
    </application>

</manifest>